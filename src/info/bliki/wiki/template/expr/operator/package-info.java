/**
 * The operator classes (i.e. for binary, prefix and postfix Operators) for the <code>{{ #expr: ... }}</code> and
 * <code>{{ #ifexpr: ... }}</code> parser template functions.
 */
package info.bliki.wiki.template.expr.operator;
